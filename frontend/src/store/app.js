const state = {
  drawer: null,
};

const getters = {};

const actions = {};

const mutations = {
  drawer(store, drawer) {
    store.drawer = drawer;
  },
};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
