const state = {
  drawer: null,
}

const getters = {}

const actions = {}

const mutations = {
  drawer(state, drawer) {
    state.drawer = drawer
  }
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
